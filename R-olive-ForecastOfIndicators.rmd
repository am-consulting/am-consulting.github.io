---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'ForecastOfIndicators';indicator <-'ServicesProducerPriceIndex';rmarkdown::render(inputFile,encoding = encoding,output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'_',indicator,'.html')) })

title : "Forecast of Financial and Economic Indicators"
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 6
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<script src="http://knowledgevault.saecanet.com/components/JavascriptCode.js"></script>
<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options,echo=F,cache=F}
options(width = 1000,scipen = 999)
```

```{r warning=F,error=F,message=F,results='hide',echo=F}
dateFormat <- '%Y-%m'
windowsFonts(Meiryo = windowsFont("Meiryo"))
commonTitle <- '経済･金融指標の将来予測:'
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fileName <- 
  'functionList.csv'
baseURL <- 
  'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/'
functionList <- 
  read.csv(
    file = paste0(baseURL,'csv/',fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    RCurl::getURL(paste0(baseURL,functionList[iii,1]),ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,results='hide',echo=F}
if(indicator=='ServicesProducerPriceIndex'){
  importAMCC <- 'importServicesProducerPriceIndex-2017.r'
  script <-
    RCurl::getURL(paste0(baseURL,importAMCC),ssl.verifypeer = F)
  eval(parse(text = script))
  obj <- 
    na.omit(ServicesProducerPriceIndex[,c(1,2)])
  dataTitle <- 
    paste0(commonTitle,'企業向けサービス価格指数:',format(tail(obj[,1],1),dateFormat))
  dataSource <- '日本銀行'
}

if(indicator=='UNRATE'){
  library(quantmod)
  buf0 <-
    getSymbols(Symbols = 'UNRATE',src = 'FRED',auto.assign = F)
  buf1 <-
    na.omit(data.frame(Date = index(buf0),buf0,check.names = F,
                       stringsAsFactors = F,row.names = NULL))
  colnames(buf1)[2] <- 'Civilian Unemployment Rate In U.S.A'
  obj <- buf1
  dataTitle <- 
    paste0(commonTitle,'米国の失業率:',format(tail(obj[,1],1),dateFormat))
  dataSource <- 'U.S. Bureau of Labor Statistics'
}
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Financial transactions and future prospects based on this page are prohibited.
- 本ページを根拠とした金融取引,将来予測を禁止します。

***

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_validation <- function(obj,arfima = 0,plotV = 1,plotF = 1){
  tailN <- c(nrow(obj),120,60,36)
  buf <-
    fun_ArimaTest(obj = obj,tailN = tailN,arfima = arfima,plotV = plotV,plotF = plotF)
  forecastResult <- buf$forecastByArima
  for(iii in seq(length(forecastResult))){
    tmp <- head(forecastResult[[iii]],1)
    tmp$Tail <- tailN[iii]
    if(iii==1){resultDF <- tmp}else{resultDF <- rbind(resultDF,tmp)}
  }
  resultDF <- resultDF[,c(8,9,1:7)]
  return(resultDF)
}
```

#### 最新結果の検証

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_latestResult(obj = obj,dateFormat = dateFormat)
cat('<div style="text-align:center"><h5>',latestResult,'</h5></div>\n\n')
```

- ARIMA

```{r warning=F,error=F,message=F,echo=F,results='hide'}
resultDF <- fun_validation(obj = head(obj,-1),arfima = 0,plotV = 1,plotF = 0)
```

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = resultDF)
```

***

- ARFIMA

```{r warning=F,error=F,message=F,echo=F,results='hide'}
resultDF <- fun_validation(obj = head(obj,-1),arfima = 1,plotV = 1,plotF = 0)
```

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = resultDF)
```

***

#### 将来予測

- ARIMA

```{r warning=F,error=F,message=F,echo=F,results='hide'}
resultDF <- fun_validation(obj = obj,arfima = 0,plotV = 0,plotF = 1)
```

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = resultDF)
```

***

- ARFIMA

```{r warning=F,error=F,message=F,echo=F,results='hide'}
resultDF <- fun_validation(obj = obj,arfima = 1,plotV = 0,plotF = 1)
```

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = resultDF)
```

***

#### ローリング線形回帰

```{r warning=F,error=F,message=F,echo=F,results='hold'}
resultDF <- 
  fun_changesInSlope(obj = obj,h = 24)
changeDate <- 
  resultDF$changesInSlope[,1]
changeDateDF <-
  data.frame(format(changeDate,dateFormat),c(0,diff(year(changeDate)*12+month(changeDate))),
             stringsAsFactors = F)
colnames(changeDateDF) <-
  c('Point of Change','Month Difference')
changeDateDF
cat('\n\n')
summary(tail(changeDateDF[,2],-1))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_updateURLcsv()
```

<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
