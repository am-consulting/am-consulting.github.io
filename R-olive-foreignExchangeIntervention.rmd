---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'foreignExchangeIntervention';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : 'Foreign Exchange Intervention Operations'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 4
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers 
    includes:
       in_header: insertHeader.html
    self_contained: false
---

```{r setup, include=FALSE}
tag <-
  read.csv(file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/components/insertHeader.csv',header = F,fileEncoding = 'UTF8')
tagTxt <- ''
for(rrr in 1:nrow(tag)){
  tagTxt <- paste0(tagTxt,tag[rrr,1],'\n')
}
insertFile <- file("insertHeader.html")
writeLines(text = tagTxt,con = insertFile)
close(insertFile)
```

<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r warning = F, error = F, message = F, echo = F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r set-options, echo=F, cache=F}
options(width = 1000)
```

```{r warning=F,error=F,message=F,echo=F}
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/importForeignExchangeIntervention.r",
    ssl.verifypeer = FALSE
  )
eval(parse(text = script))
```

```{r,warning=F,error=F,message=F,echo=F}
# # Reference http://stackoverflow.com/questions/32377291/insert-date-in-filename-while-knitting-document-using-rstudio-knit-button
# library(DT)
library(lubridate)
# library(pipeR)
```

```{r,warning=F,error=F,message=F,echo=F}
dataSet <- 
  ForeignExchangeIntervention
yyyy <- 
  as.numeric(substring(dataSet[,2],3,4)) + 1988
mm <- 
  as.numeric(gsub('月','',substring(dataSet[,2],6,7)))
dataSet$`期間(西暦)` <- 
  format(as.Date(paste0(yyyy,'-',mm,'-1')) %m+% months(1),'%Y年%m月')
dataSetDT <- 
  dataSet
dataSetDT[is.na(as.numeric(dataSetDT[,4])),4] <- -9999
dataSetDT[,4] <- 
  as.numeric(dataSetDT[,4])
dataSetNA <- 
  dataSet[dataSetDT[,4] == -9999,]
dataSetIV <- 
  subset(dataSetDT, dataSetDT[,4] != -9999 & dataSetDT[,4] != 0)
commonCaption <- 
  paste0(tail(dataSetDT[,2],1),' - ',head(dataSetDT[,2],1))
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
fun_latestResult(obj = dataSetDT,
                 objColumn = 4,
                 orderDirection = 1,
                 orderColumn = 1,
                 dateColumn = 2,
                 dateFormat = 0)
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
dataTitle <- paste0('外国為替平衡操作の実施状況(月次ベース):',latestResult)
dataSource <- '財務省'
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source : http://www.mof.go.jp/international_policy/reference/feio/monthly/index.htm

***

```{r warning=F, error=F, message=F, echo=F, results='asis'}
fun_dataTable(obj = dataSetDT,
              dateFormat = 0,
              title = commonCaption,
              orderDirection = 'asc',
              leftcolumns = 2)
```

- -9999は404 Not Foundを意味する

***

- うち外国為替平衡操作実施月とその操作額(億円)

```{r warning=F, error=F, message=F, echo=F, results='asis'}
fun_markdownTable(obj = dataSetIV)
cat(paste0('<h5><b>合計(兆円) : ',sum(dataSetIV[,4])/10^4,'</b></h5>'))
```

***

- うち http://www.mof.go.jp/international_policy/reference/feio/monthly/index.htm からのリンク先が'404 Not Found'となる月。

```{r warning=F, error=F, message=F, echo=F, results='asis'}
if(nrow(dataSetNA)!=0){
fun_markdownTable(obj = dataSetNA)
}else{
  cat('<div align="center"><b>なし</b></div>')
}
```

```{r warning=F, error=F, message=F, echo=F, results='hide'}
cat('#### 直近2ヶ月間の為替平衡操作額')
fun_markdownTable(obj = fun_transposeDataFrame(obj = head(dataSetDT[,c(2,4)],2),dateFormat = 0,colName = '期間',tailN = 2))
```

***

```{r warning=F, error=F, message=F, echo=F, results='asis'}
Date <-
  as.Date(paste0(as.numeric(gsub('平成([0-9]+)年([0-9]+)月.+','\\1',ForeignExchangeIntervention[,2]))+1988,
                 '-',
                 gsub('平成([0-9]+)年([0-9]+)月.+','\\2',ForeignExchangeIntervention[,2]),
                 '-1')) %m+% months(1)
obj <-
  data.frame(Date,ForeignExchangeIntervention[,4,drop=F],
             check.names = F,stringsAsFactors = F,row.names = NULL)
obj[,2] <- as.numeric(obj[,2])
n <- 1
fun_generateKnit(objDF = obj,plotNum = n,barPlot = 1,quantiles = 2)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))

```

***

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_preparePlot()
pngFile <-
  paste0(htmlName, "1.png")
png(file = pngFile, width = 1000, height = 600)
##################
fun_plotTimeSeries(obj = obj,
                   typeL = 'h',dateFormat = '%Y年%m月',
                   chartType = 0,dataTitle = dataTitle,
                   dataSource = dataSource,fitLcolor = 0,lwdL=2,mar=c(4,5,6,2))
fun_addTxtToChart(obj = obj,cexTxt = 1,funType = 'min',col = 'black')
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_outputMD(tags = '財務省,外国為替平衡操作,為替介入',
             objDF = dataSetIV[,-1],
             htmlName = htmlName,
             image1 = 1,image2 = 0,
             title = dataTitle,
             tableTitle = '外国為替平衡操作実施月とその操作額(億円)',
             tableName = '外国為替平衡操作実施月とその操作額(億円)')
fun_updateURLcsv()
```

<script>MakeNegative()</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
