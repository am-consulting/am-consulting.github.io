---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'ServicesProducerPriceIndexInJapan';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : 'Services Producer Price Index In Japan'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 5
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<script src="http://knowledgevault.saecanet.com/components/JavascriptCode.js"></script>
<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 1000)
```

```{r warning = F, error = F, message = F, echo = F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
importAMCC <-
  c('importMonetarybaseOfJapan-2017.r',
    'importServicesProducerPriceIndex-2017.r')
for(iii in 1:length(importAMCC)){
  script <-
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/",
                  importAMCC[iii]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,echo=F,results='hold'}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = ServicesProducerPriceIndex,dateFormat = dateFormat)
dataTitle <- 
  paste0('日本の企業向けサービス価格指数:',latestResult)
dataSource <- '日本銀行'
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.stat-search.boj.or.jp/ssi/mtshtml/pr02_m_1.html

***

```{r warning=F, error=F, message=F, echo=F, results='asis', fig.height=4}
n <- 2
obj <- 
  ServicesProducerPriceIndex[,c(1,n)]
fun_generateKnit(objDF = obj,
                 plotNum = n,
                 group = 0,
                 quantiles = 2,
                 barPlot = 1,
                 dygraphTitle = colnames(obj)[2])
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', ConsumptionTax)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', event_dygraph_point,' %>%  ',event_dygraph_range)))
```

- 基本統計量:`r paste0(format(range(obj[,1]),dateFormat),collapse = '~')`

```{r warning=F, error=F, message=F, echo=F, results='hold'}


apply(obj[,-1,drop=F],2,summary)
```

***

```{r warning=F, error=F, message=F, echo=F, results='asis', fig.height=4}
n <- 4
obj <- 
  ServicesProducerPriceIndex[,c(1,n)]
fun_generateKnit(objDF = obj,
                 plotNum = n,
                 group = 0,
                 quantiles = 2,
                 barPlot = 0,
                 dygraphTitle = colnames(obj)[2])
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', ConsumptionTax)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', event_dygraph_point,' %>%  ',event_dygraph_range)))
```

- 基本統計量:`r paste0(format(range(obj[,1]),dateFormat),collapse = '~')`

```{r warning=F, error=F, message=F, echo=F, results='hold'}
apply(obj[,-1,drop=F],2,summary)
```

***
#### 自己相関係数

```{r warning=F, error=F, message=F, echo=F, results='hold'}
dammy <- fun_acf(obj = na.omit(ServicesProducerPriceIndex),lag.max = 12*10,objColumn = 2)
```

***
#### マネタリーベースとの比較
##### 単位根検定
- 原系列

```{r warning=F, error=F, message=F, echo=F, results='hold'}
dataSet <- 
  na.omit(merge(monetaryBase_Japan[,c(1,6)],ServicesProducerPriceIndex[,c(1,4)],by='Date'))
apply(dataSet[,-1],2,adf.test)
```

- 一次差分

```{r warning=F, error=F, message=F, echo=F, results='hold'}
diffDF <- fun_makeDiffTable(obj = dataSet)
apply(diffDF[,-1],2,adf.test)
```

***
#### 原系列同士の共和分検定

```{r warning=F, error=F, message=F, echo=F, results='hold', fig.height=5}
fun_cointegration(obj = dataSet)
```

***
#### 一次差分同士の相互相関係数

```{r warning=F, error=F, message=F, echo=F, results='asis', fig.height=5}
par(mfrow = c(1,1), family = 'Meiryo', mar = c(5,5,6,4))
ccfResult <- 
  ccf(x = diffDF[,2], 
      y = diffDF[,3], 
      main = paste0(colnames(diffDF)[c(2,3)], collapse = '\n×\n'))
panel.first = grid(nx = NULL, ny = NULL, lty = 2, equilogs = T)
text(ccfResult$lag, ccfResult$acf, round(ccfResult$acf,2))
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_dataTable(obj = tail(ServicesProducerPriceIndex,12*3), 
              title = dataTitle,
              dateFormat = dateFormat)
```

***
#### 前年比の正負が変化した時点:前年比±0(%)は正(プラス)とする。

```{r warning=F,error=F,message=F,echo=F,results='asis'}
objColumn <- 2
obj <- na.omit(ServicesProducerPriceIndex[,c(1,objColumn)])
fun_turningPoint(obj = obj,
                 dateFormat = dateFormat)
fun_markdownTable(obj = objPNinclude0)
cat(paste0('<h4>', tail(objPNinclude0[,1],1),'から',
           format(tail(obj[,1],1), dateFormat),'まで',
           durationInclude0,'ヶ月連続で',
           ifelse(tail(objPNinclude0[,2],1) < 0,'マイナス値','プラス値'),'</h4>'))
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
forecastResultDF <- fun_arfima(obj = ServicesProducerPriceIndex,objColumn = 4)
Date <- 
  tail(seq(from = tail(ServicesProducerPriceIndex[,1],1),
           by = '+1 month',
           length.out = nrow(forecastResultDF) + 1),-1)
fun_markdownTable(obj = 
                    data.frame(Date = format(Date,dateFormat), 
                               forecastResultDF, 
                               stringsAsFactors = F, check.names = F))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_markdownTable(obj = 
                    fun_transposeDataFrame(obj = 
                                             ServicesProducerPriceIndex,dateFormat = dateFormat,
                                           tailN = 3))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
obj <- ServicesProducerPriceIndex[,c(1,2)]
fun_PlotStrawAnd4charts(obj = obj,varType = 2,dateFormat = dateFormat)
dev.off();dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_outputMD(tags = '日本銀行, 企業向けサービス価格指数',
             objDF = fun_summaryByName(obj = obj,dateFormat = dateFormat),
             htmlName = htmlName,
             image1 = 1,image2 = 1,
             title = dataTitle,
             tableTitle = colnames(obj)[2])
fun_updateURLcsv()
```

***
<script>MakeNegative()</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
