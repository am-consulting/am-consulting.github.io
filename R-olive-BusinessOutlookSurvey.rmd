---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');htmlName<-'BusinessOutlookSurvey';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[4,1],'am-consulting.co.jp-',htmlName,'.html')) })

title : 'Business Outlook Survey In Japan'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 4
    fig_width: 9  
    keep_md: no
    md_extensions: -ascii_identifiers 
    includes:
       in_header: insertHeader.html
    self_contained: false
---

```{r setup, include=FALSE}
tag <-
  read.csv(file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/components/insertHeader.csv',header = F,fileEncoding = 'UTF8')
tagTxt <- ''
for(rrr in 1:nrow(tag)){
  tagTxt <- paste0(tagTxt,tag[rrr,1],'\n')
}
insertFile <- file("insertHeader.html")
writeLines(text = tagTxt,con = insertFile)
close(insertFile)
```

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r warning = F, error = F, message = F, echo = F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,echo=F}
library(XLConnect);library(Nippon)
username <- Sys.info()['user']
pathOutput <- paste0("C:/Users/", username, "/Desktop/R_Data_Write/")
setwd(pathOutput)
fileName <- 'jikeiretsu.xlsx'
# 2017-09からファイルがxlsからxlsxへと変更された。両者のセル構成に違いはない。xlsもダウンロード可能であるため折に触れ確認すること(2017-09-13)。
baseURL <- 'http://www.mof.go.jp/pri/reference/bos/results/'
if(!file.exists(paste0(pathOutput, fileName))) {
  download.file(paste0(baseURL,fileName), fileName , mode = 'wb')
}
buf0 <- readWorksheetFromFile(paste0(pathOutput, fileName), sheet = 1, header = F)
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
buf <- buf0
bsiTitle <- vector()
origbsiData <- bsiData <- list()
buf[,1] <- unlist(lapply(buf[,1], zen2han))
buf[,2] <- unlist(lapply(buf[,2], zen2han))
bsiCCC <- c(grep('bsi', buf[,1], ignore.case = T)[-1], nrow(buf)+1)
for(iii in 1:(length(bsiCCC)-1)){
  origbsiData[[iii]] <- buf[bsiCCC[iii]:(bsiCCC[iii+1]-1),]
  bsiTitle <- c(bsiTitle,origbsiData[[iii]][1,1])
  print(head(origbsiData[[iii]][,1:5,drop=F]))
  print(tail(origbsiData[[iii]][,1:5,drop=F]))
}  
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
for(iii in 1:length(origbsiData)){
  tmp0 <- origbsiData[[iii]]
  tmp0 <- tmp0[-1,]
  tmp0[,1] <- as.numeric(gsub('年','',tmp0[,1]))
  tmp0[,1] <- unlist(lapply(tmp0[,1],function(x){if(is.numeric(x)){x+1988}else{NA}}))
  buf <- NA
  for(rrr in 1:nrow(tmp0)){
    if(!is.na(tmp0[rrr,1])){buf <- tmp0[rrr,1]}
    tmp0[rrr,1] <- buf
  }
  tmp0[,2] <- as.numeric(substring(gsub('月','',tmp0[,2]), unlist(gregexpr('~',gsub('月','',tmp0[,2])))+1))
  tmp0[,1] <- paste0(tmp0[,1],'-',tmp0[,2],'-1')
  for(rrr in 1:2){
    buf <- NA
    for(ccc in 1:ncol(tmp0)){
      if(!is.na(tmp0[rrr,ccc])){buf<-tmp0[rrr,ccc]}
      tmp0[rrr,ccc]<-buf
    }
  }
  colnames(tmp0) <- paste0(tmp0[1,],'-',tmp0[2,],'-',tmp0[3,])
  rrrRemove <- which(sapply(as.numeric(tmp0[,2]),function(x){!is.na(x)}))[1]-1
  tmp0<-tmp0[-c(1:rrrRemove),-2]
  tmp0[,-1]<-apply(tmp0[,-1],2,function(x)as.numeric(gsub('▲ ','-',x)))
  colnames(tmp0)[1] <- 'Date'
  tmp0[,1] <- as.Date(tmp0[,1])
  bsiData[[iii]] <- na.omit(tmp0)
  print(head(bsiData[[iii]][,1:5,drop=F]))
  print(tail(bsiData[[iii]][,1:5,drop=F]))
}
```

```{r warning=F, error=F, message=F, echo=F, results='hold'}
# csv出力パート
scriptFile <- 'R-writeCSVtoFolder.r'
script <-
  RCurl::getURL(
    paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/",
           scriptFile),
    ssl.verifypeer = F)
eval(parse(text = script))
for(iii in seq(length(bsiData))){
  obj <- bsiData[[iii]]
  csvFileName <- gsub('[0-9]+\\.(.+)\\(.+\\)','\\1',bsiTitle[iii])
  fun_writeCSVtoFolder(objData = obj,dataType = 1,
                       csvFileName = paste0('法人企業景気予測調査_',csvFileName))
}
# csv出力パート
```

```{r warning=F, error=F, message=F, echo=F, results='hold'}
dateFormat <- '%Y年%m月期'
dataTitle <- 
  paste0('法人企業景気予測調査:景況判断、売上高判断、経常利益判断他:BSI項目',
         format(tail(bsiData[[1]][,1],1),dateFormat))
dataSource <- '内閣府'
```
***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.esri.cao.go.jp/jp/stat/hojin/menu_hojin.html

***
```{r warning=F,error=F,message=F,echo=F,results='hide'}
windowsFonts(Meiryo=windowsFont("Meiryo"))
options(scipen=9999)
```

```{r warning=F, error=F, message=F, echo=F, results='asis'}
texDygraph <- vector()
for(iii in 1:length(bsiData)){
  cat('<h4>',bsiTitle[iii],'</h4>\n')
  fun_markdownTable(obj = fun_transposeDataFrame(bsiData[[iii]],
                                                       tailN = 5,
                                                       dateFormat = dateFormat))
  cat('\n<hr>\n')
  obj <- 
    bsiData[[iii]][c(1,grep('全産業-当期',colnames(bsiData[[iii]])))]
  fun_generateKnit(objDF = obj, 
                   plotNum = iii, 
                   group = 1, 
                   dygraphTitle = paste0(bsiTitle[iii]))
  assign(paste0('knit_expanded', iii),
         paste0("\n```{r warning=F, error=F, message=F, echo=F, results='asis'}\n\n",
                "eval(parse(text = paste0('dygraphPlot", 
                iii,
                " %>% ', primeMinisterOfJapan_dygraph , '%>%  dyLimit(0, color = \"red\")')))",
                "\n\ncat('<hr>')\n\n```"),
         envir = .GlobalEnv)
  texDygraph <- 
    c(texDygraph, paste0('knit_expanded', iii))
}
```

`r paste(knitr::knit(text = sapply(texDygraph,get)), collapse = '\n')`

```{r warning=F,error=F,message=F,echo=F,results='hide'}
username <- Sys.info()['user']
pathToFile <- paste0('C:/Users/', username,'/Desktop/pathToCSV/')
setwd(pathToFile)
fileName <- 'defaultPath.csv'
buf <-
  read.csv(file = fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,
           fileEncoding = 'utf-8',quote = "\"")
pathOutputTOchart <- paste0("C:/Users/", username, buf[4,1])
setwd(pathOutputTOchart)
#---------------------------------------------------
fun_plot <- function(){
  par(mar=c(4,3,4,1),family='Meiryo',font.main=1,cex.main=1)
  plot(x = obj[,1],y = obj[,2],type='o',pch=20,cex=1.5,ylim=ylim,xlab='',ylab='',
       main=paste0(bsiTitle[iii],'\nSource:',dataSource),
       col=lineCol[1],panel.first = grid(nx = NULL,ny = NULL,lty = 2,equilogs = T))
  lines(x = obj[,1],y = obj[,3],type='o',pch=20,cex=1.5,col=lineCol[2])
  lines(x = obj[,1],y = obj[,4],type='o',pch=20,cex=1.5,col=lineCol[3])
  graphics::legend(x = 'topleft',col = lineCol,lty = 1,legend = colnames(obj)[-1],
                   cex = 1.2,bty = 'n',lwd = 3)
  fun_addTxtToChart(obj = obj,funType = 'min',objCol = 2)
  abline(h=0)
}
iii <- 14
lineCol <- c('blue','red','#708090')
obj0 <- bsiData[[iii]]
pngFile <-
  paste0(htmlName, "1.png")
png(file = pngFile, width = 1000, height = 600)
obj <- obj0[,c(1,grep('-非製造業-翌々期',colnames(obj0)))]
ylim <- c(min(obj[,-1]),max(obj[,-1]))
fun_plot()
dev.off()
pngFile <-
  paste0(htmlName, "2.png")
png(file = pngFile, width = 1000, height = 600)
obj <- obj0[,c(1,grep('-製造業-翌々期',colnames(obj0)))]
ylim <- c(min(obj[,-1]),max(obj[,-1]))
fun_plot()
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
obj <- obj0[,c(1,grep('製造業-翌々期',colnames(obj0)))]
fun_outputMD(tags = '法人企業景気予測調査,内閣府',
             objDF = fun_transposeDataFrame(obj = obj,dateFormat = dateFormat),
             htmlName = htmlName,
             image1 = 1,image2 = 1,
             title = dataTitle,
             tableTitle = '翌々期',
             tableName = bsiTitle[iii],
             buf.row = 4)
fun_updateURLcsv()
fun_updateURLcsv.olive()
```

<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
