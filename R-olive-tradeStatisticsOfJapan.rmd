---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-tradeStatisticsOfJAPAN.html')) })

title : 'Trade Statistics Of JAPAN'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 5
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers 
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<style>.main-container {max-width: 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 100)
```

```{r,warning=F,error=F,message=F,echo=F}
library(DT)
library(lubridate)
library(googleVis)
library(rAmCharts)
library(pipeR)
library(RCurl)
library(seasonal)
library(dplyr)
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/Rscript/master/importCustomExportImportData.r",
    ssl.verifypeer = FALSE
  )
eval(parse(text = script))
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 100)
```

```{r,warning=F,error=F,message=F,echo=F}
library(RCurl)
functionList <-
  read.csv(file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/functionList.csv',
           header = F,
           as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", functionList[iii,1]),
    ssl.verifypeer = F
  )
  eval(parse(text = script))
}
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = origData4,objColumn = 2,dateFormat = dateFormat)
dataTitle <- 
  paste0('財務省貿易統計:貿易収支 ', latestResult,'(単位:兆円)')
dataSource <- 
  '財務省'
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.customs.go.jp/toukei/suii/html/time.htm
- Source http://www.customs.go.jp/toukei/suii/html/data/d41ma.csv
- Source http://www.customs.go.jp/toukei/suii/html/data/d51ma.csv
- Source http://www.customs.go.jp/toukei/suii/html/data/d61ma.csv

***

```{r,warning=F,error=F,message=F,echo=F} 
funSeasonal <- function() {
  tmpSA <- dataSet
  buf01 <-
    ts(data = tmpSA[, obj01],
       start = c(year(tmpSA[1, 1]), month(tmpSA[1, 1])),
       freq = 12)
  buf02 <-
    ts(data = tmpSA[, obj02],
       start = c(year(tmpSA[1, 1]), month(tmpSA[1, 1])),
       freq = 12)
  result01 <- final(seas(buf01, na.action = na.omit))
  result02 <- final(seas(buf02, na.action = na.omit))
  tmpSA[, obj01] <- as.matrix(result01)
  tmpSA[, obj02] <- as.matrix(result02)
  dataSetSA <<- tmpSA
}
```

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj01 <- 2
obj02 <- 3
dataTail <- 12 * 20000
dataSet <- origData1
dataSet <- tail(dataSet, dataTail)
funSeasonal()
```

#### 財務省貿易統計:輸出と輸入(単位:兆円、原数値)

```{r,warning=F,error=F,message=F,results='asis',echo=F,fig.height=4}
n <- 1
fun_generateKnit(objDF = dataSet,plotNum = n,group = 0)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
```

#### 財務省貿易統計:輸出と輸入(単位:兆円、季節調整値)

```{r,warning=F,error=F,message=F,results='asis',echo=F,fig.height=4}
n <- 2
fun_generateKnit(objDF = dataSetSA,plotNum = n,group = 0)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
```

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj01 <- 2
obj02 <- 2
dataTail <- 12 * 20000
dataSet <- origData4
dataSet <- tail(dataSet, dataTail)
funSeasonal()
```

#### 財務省貿易統計:貿易収支(単位:兆円、原数値)

```{r,warning=F,error=F,message=F,results='asis',echo=F,fig.height=4}
n <- 3
fun_generateKnit(objDF = dataSet,plotNum = n,group = 0,barPlot = 1)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
```

#### 財務省貿易統計:貿易収支(単位:兆円、季節調整値)

```{r,warning=F,error=F,message=F,results='asis',echo=F,fig.height=4}
n <- 4
fun_generateKnit(objDF = dataSetSA,plotNum = n,group = 0,barPlot = 1)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
```

```{r,warning=F,error=F,message=F,echo=F}
fungvisTable <- function(obj) {
  PopTable <- gvisTable(      obj, 
    options = list(width = 2000, height = 'automatic',  page = 'enable' ,  pageSize=10)
  )
  plot(PopTable,tag='chart')
}
```

```{r,warning=F,error=F,message=F,results='asis',echo=F}
dataSet<-merge(origData1,origData4)
dataSet[,-1]<-round(dataSet[,-1],4)
fun_dataTable(obj = dataSet, title = paste0(dataTitle,'(原数値、単位:兆円)'))
```

```{r,warning=F,error=F,message=F,echo=F} 
funPlotBoxplotAmcharts <- function(obj, bigTitle = '') {
  amBoxplot(obj, horiz = T, names = colnames(obj)) %>>% amOptions(theme = "none" , main = bigTitle)
}
```

***

```{r,warning=F,error=F,message=F,echo=F} 
funPlotBoxplotAmcharts(obj = dataSet[, -1],
                       bigTitle = paste0(
                       '財務省貿易統計:基本統計量:原数値(単位:兆円)\n',
                       head(dataSet[, 1], 1),
                       '~',
                       tail(dataSet[, 1], 1)
                       ))
```

***

```{r,warning=F,error=F,message=F,results='asis',echo=F}
compareData<-merge(origData1,origData4)
compareData[,-1]<-round(compareData[,-1],4)
compareData01<-subset(compareData,as.Date('2009-09-01')<=compareData[,1] & compareData[,1]<=as.Date('2012-12-1'))
compareData02<-subset(compareData,as.Date('2013-01-01')<=compareData[,1])
compareData01[,1]<-format(compareData01[,1],'%Y-%m')
compareData02[,1]<-format(compareData02[,1],'%Y-%m')
colnames(compareData01)<-paste0(colnames(compareData01),':民主党政権')
colnames(compareData02)<-paste0(colnames(compareData02),':自民党政権')
compareData0<-qpcR:::cbind.na(compareData01[,-1], compareData02[,-1])
funPlotBoxplotAmcharts(obj = compareData0,
                       bigTitle = paste0(
                       '財務省貿易統計:基本統計量:原数値(単位:兆円)\n',
                       '民主党政権:',head(compareData01[, 1], 1),'~',tail(compareData01[, 1], 1),
                       '\n自民党政権:',head(compareData02[, 1], 1),'~',tail(compareData02[, 1], 1)
                       ))
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
apply(compareData0,2,function(x)summary(na.omit(x)))
```

#### 直近`r tailN <-3;tailN`ヶ月の商品別輸出額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
buf <- tail(na.omit(origData2), tailN)
buf0 <- t(buf)
colnames(buf0) <- format(as.Date(buf0[1, ]), dateFormat)
buf0 <- buf0[-1, ]
buf1 <- apply(buf0, 2, as.numeric)
buf1 <- apply(buf1, 2, function(x) {round(x, 0)})
expItem <- data.frame(ID = seq(1,nrow(buf1)), Item = row.names(buf0), buf1, check.names = F)
colnames(expItem)[-c(1,2)] <- paste0(colnames(expItem)[-c(1,2)],'(億円)')
fun_dataTable(obj = expItem,dateFormat = 0,orderDirection = 'asc')
```

***
#### 直近`r tailN`ヶ月の商品別輸入額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
buf <- tail(na.omit(origData3), tailN)
buf0 <- t(buf)
colnames(buf0) <- format(as.Date(buf0[1, ]), dateFormat)
buf0 <- buf0[-1, ]
buf1 <- apply(buf0, 2, as.numeric)
buf1 <- apply(buf1, 2, function(x) {round(x, 0)})
impItem <- data.frame(ID = seq(1,nrow(buf1)), Item = row.names(buf0), buf1, check.names = F)
colnames(impItem)[-c(1,2)] <- paste0(colnames(impItem)[-c(1,2)],'(億円)')
fun_dataTable(obj = impItem,dateFormat = 0,orderDirection = 'asc')
```

***
<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
