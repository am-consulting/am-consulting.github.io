---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'measuresOfUnderlyingInflation';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : '基調的なインフレ率を捕捉するための指標'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 5
    fig_width: 9
    keep_md: no
    md_extensions: -ascii_identifiers 
    includes:
       in_header: insertHeader.html
    self_contained: false
---

```{r setup, include=FALSE}
tag <-
  read.csv(file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/components/insertHeader.csv',header = F,fileEncoding = 'UTF8')
tagTxt <- ''
for(rrr in 1:nrow(tag)){
  tagTxt <- paste0(tagTxt,tag[rrr,1],'\n')
}
insertFile <- file("insertHeader.html")
writeLines(text = tagTxt,con = insertFile)
close(insertFile)
```

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 900)
```

```{r warning=F, error=F, message=F, echo=F}
library(RCurl)
```

```{r warning=F, error=F, message=F, echo=F}
# ―,ソ,噂,欺,圭,構,蚕,十,申,貼,能,表,暴,予,禄,兔
rFile <- 
  c('importMeasuresOfUnderlyingInflation-2017.r','importMonetarybaseOfJapan-2017.r')
for(iii in 1:length(rFile)){
script <- 
 getURL(paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/',rFile[iii]),
        ssl.verifypeer = F)
eval(parse(text = script))
}
```

```{r,warning=F,error=F,message=F,echo=F}
functionList <-
  read.csv(
    file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/functionList.csv',
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(
      paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", functionList[iii,1]),
      ssl.verifypeer = F
      )
  eval(parse(text = script))
}
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = measuresOfUnderlyingInflation,dateFormat = dateFormat)
dataTitle <- paste0('基調的なインフレ率を捕捉するための指標:', latestResult)
dataSource <- '日本銀行'
windowsFonts(Meiryo=windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.boj.or.jp/research/research_data/cpi/index.htm/

***

```{r warning=F, error=F, message=F, echo=F}
fun_plotTimeSeries0 <- function(obj,objColumn){
  par(mar=c(3,3,3,2),family='Meiryo',font.main = 1,cex.main = 1)
  yMax <- max(obj[,objColumn],na.rm = T)
  yMin <- min(obj[,objColumn],na.rm = T)
  titleText <- 
    substring(colnames(obj)[objColumn][1], 1, regexpr(':[0-9]+?',colnames(obj)[objColumn][1])-1)
  plot(obj[,c(1,2)],xlim= range(obj[,1]), ylim=c(yMin,yMax), ylab="", xlab="", type='l',col = 'white',
       main = paste0(titleText, '\n', paste0(format(range(obj[,1]), dateFormat), collapse = '~')))
  grid(nx = NA, ny = NULL, lty = 2, equilogs = T)
  ticks <- as.Date(c(paste0(unique(year(obj[,1])),'-1-1'),
                     paste0(unique(year(obj[,1])),'-6-30')))
  abline(v = ticks,col = "lightgray",lty = 2)
  cnt <- 1
  for(iii in objColumn){
    lines(obj[,1],obj[,iii],type='h',col=cnt,xaxt='n')
    cnt <- cnt +1
  }
  # axis.Date(side=1, at=obj[,1],format=dateFormat,padj=1,cex.axis=1.0) 
  # panel.first = grid(nx = NULL, ny = NULL, lty = 2, equilogs = T)
  graphics::legend('bottomleft',col=1:cnt,lty=1,
                   legend=colnames(obj)[objColumn],cex=1,bty='n',lwd = 2)
  # rect part
  rectDateL <- as.numeric(as.Date('2009-09-16'))
  rectDateR <- as.numeric(as.Date('2012-12-26'))
  if(as.numeric(obj[1,1]) < rectDateR){
    rectText <- '民主党政権'
    rect(xleft = rectDateL, ybottom = par('usr')[3], 
         xright = rectDateR, ytop = par('usr')[4], col = 'dimgray', density = 5, angle = 45)
    text(rectDateL, par('usr')[4], labels = paste0('\n\n',rectText), pos = 4)
  }
  # rect part
}
```

```{r warning=F, error=F, message=F, echo=F}
objColumn <- grep('刈込平均',colnames(measuresOfUnderlyingInflation))
fun_plotTimeSeries0(obj = measuresOfUnderlyingInflation, objColumn = objColumn)
```

***

```{r warning=F, error=F, message=F, echo=F}
objColumn <- grep('ポイント',colnames(measuresOfUnderlyingInflation))
fun_plotTimeSeries0(obj = measuresOfUnderlyingInflation, objColumn = objColumn)
```

***

```{r warning=F, error=F, message=F, echo=F, results='asis'}
fun_markdownTable(obj = na.omit(fun_transposeDataFrame(obj = measuresOfUnderlyingInflation,
                                                       dateFormat = dateFormat)))
```

***

#### マネタリーベースとの比較

```{r warning=F, error=F, message=F, echo=F, fig.height=6, results='hide'}
allData <- 
  merge(measuresOfUnderlyingInflation,monetaryBase_Japan,by='Date')
colnames(allData)
cexText <- 0.8
srtText <- 0
posText <- 3 
offsetText <- 0.2
dateFormat <- '%Y-%m'
cexPlot <- 1.5
cexLab <- 0.9
cexMain <- 0.9
```

```{r warning=F, error=F, message=F, echo=F, fig.height=6, results='hide'}
scatterData <- na.omit(allData[,c(1,29,2)])
fun_scatterPlot(obj = scatterData, 
                cexText = cexText, 
                srtText = srtText, 
                posText = posText, 
                offsetText = offsetText,
                dataSource = dataSource,
                dateFormat = dateFormat,
                cexPlot = cexPlot,
                cexLab = cexLab,
                cexMain = cexMain)
```

***

```{r warning=F, error=F, message=F, echo=F, fig.height=6, results='hide'}
scatterData <- na.omit(allData[,c(1,30,2)])
fun_scatterPlot(obj = scatterData, 
                cexText = cexText, 
                srtText = srtText, 
                posText = posText, 
                offsetText = offsetText,
                dataSource = dataSource,
                dateFormat = dateFormat,
                cexPlot = cexPlot,
                cexLab = cexLab,
                cexMain = cexMain)
```

***

```{r warning=F, error=F, message=F, echo=F, fig.height=6, results='hide'}
scatterData <- na.omit(allData[,c(1,32,2)])
fun_scatterPlot(obj = scatterData, 
                cexText = cexText, 
                srtText = srtText, 
                posText = posText, 
                offsetText = offsetText,
                dataSource = dataSource,
                dateFormat = dateFormat,
                cexPlot = cexPlot,
                cexLab = cexLab,
                cexMain = cexMain)
```

```{r warning=F, error=F, message=F, echo=F, results='hide'}
fun_markdownTable(obj = na.omit(fun_transposeDataFrame(obj = measuresOfUnderlyingInflation,
                                                       dateFormat = '%Y年%m月')))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_preparePlot()
pngFile <- paste0(htmlName, "1.png")
png(file = pngFile, width = 1000, height = 600)
######  
obj <- na.omit(allData[,c(1,14,15)])
fun_plotTimeSeries(obj = obj,chartType = 1,typeL = 'o',typeR = 'o',dataTitle = dataTitle,dataSource = dataSource,mar = c(4,4,5,1),needLefLab = 0,cexL = 2,cexR = 2,cex.main = 1,dateFormat = '%Y年%m月')
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_outputMD(tags = '基調的なインフレ率を捕捉するための指標',
             objDF = fun_transposeDataFrame(obj = obj,tailN = 5,dateFormat = '%Y年%m月'),
             htmlName = htmlName,
             image1 = 1,image2 = 0,
             title = dataTitle,
             tableTitle = '直近5ヶ月',
             tableName = paste0(colnames(obj)[2:3],collapse = '×'))
fun_updateURLcsv()
```

***
<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
