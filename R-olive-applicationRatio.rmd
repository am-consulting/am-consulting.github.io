---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-applicationRatioInJapan.html')) })
title : "Job Opening-to-Application Ratio In Japan"
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 4
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers 
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<script src="http://knowledgevault.saecanet.com/components/JavascriptCode.js"></script>
<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 100)
```

```{r warning=F, error=F, message=F, echo=F}
library(XLConnect);library(RCurl);library(lubridate);library(Nippon);library(knitr)
```

```{r warning=F, error=F, message=F, echo=F}
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/importEmploymentReferralsForGeneralWorkers.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))
```

```{r warning = F, error = F, message = F, echo = F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r warning=F, error=F, message=F, echo=F, results='hide'}
colnames(jobOpenings) <- gsub('\n','',unlist(lapply(colnames(jobOpenings),zen2han)))
dataSet0 <- na.omit(jobOpenings[,-2])
colnames(dataSet0)
objCC <- c(1,grep('率',colnames(dataSet0)))
dataSet0[,-objCC] <- apply(dataSet0[,-objCC],2,function(x){x*10^-4})
colnames(dataSet0)[-objCC] <- gsub('\\(', '\\(万', colnames(dataSet0)[-objCC])
```

```{r warning=F, error=F, message=F, echo=F, results='hide'}
if(length(unique(month(dataSet0[,1])))==1){
  dateFormat <- '%Y年'
}else if(length(unique(day(dataSet0[,1])))==1){
  dateFormat <- '%Y年%m月'
}else{
  dateFormat <- '%Y年%m月%d日'
}
```

```{r warning=F,error=F,message=F,echo=F,results='hold'}
fun_latestResult(obj = dataSet0,objColumn = c(2,3,4),dateFormat = dateFormat)
dataTitle <- 
  paste0('日本の一般職業紹介状況:',latestResult)
dataSource <- '厚生労働省'
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`

***

- 人数、件数の単位：万人、万件

```{r warning=F, error=F, message=F, echo=F}
objTitle <- '倍率\\(倍\\)-季節調整'
n1 <- 1
bufDF <- na.omit(dataSet0[,c(1,grep(objTitle, colnames(dataSet0), ignore.case = T))])
fun_generateKnit(objDF = bufDF, plotNum = n1, colors = c('blue','red'), group = 0,
                 dygraphTitle = paste0(colnames(bufDF)[-1], collapse = ' × '))
```

```{r warning=F, error=F, message=F, echo=F}
objTitle <- '求人数\\(万人\\)-季節調整'
n2 <- 2
bufDF <- na.omit(dataSet0[,c(1,grep(objTitle, colnames(dataSet0), ignore.case = T))])
fun_generateKnit(objDF = bufDF, plotNum = n2, colors = c('blue','red'), group = 0,
                 dygraphTitle = paste0(colnames(bufDF)[-1], collapse = ' × '))
```

`r paste(knit(text = c(get(paste0('knit_expanded',n1)),get(paste0('knit_expanded',n2)))), collapse = '\n')`

```{r warning=F, error=F, message=F, echo=F, results='asis'}
fun_dataTable(obj = tail(dataSet0,12*5),dateFormat = dateFormat)
```

***
#### 前月差(単位:万人、ポイント)

```{r warning=F, error=F, message=F, echo=F, results='asis'}
dataSet <- dataSet0[,grep('Date|季節調整値',colnames(dataSet0))]
fun_markdownTable(obj = fun_transposeDataFrame(obj = fun_makeDiffTable(obj = dataSet),
                                               dateFormat = dateFormat))
```

***
#### 前年同月差(単位:万人、ポイント)

```{r warning=F, error=F, message=F, echo=F, results='asis'}
dataSet <- dataSet0[,-grep('季節調整値',colnames(dataSet0))]
fun_markdownTable(obj = fun_transposeDataFrame(obj = fun_makeDiffTable(obj = dataSet,lag = 12),
                                               dateFormat = dateFormat))
```

```{r warning=F, error=F, message=F, echo=F, results='hide'}
fun_markdownTable(obj = fun_transposeDataFrame(obj = dataSet0,
                                               dateFormat = dateFormat))
```

***
<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
