---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'JobOfferByTermsOfOccupationalCategories';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : 'Job Offer By Terms Of Occupational Categories'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 5
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<style>.main-container {max-width : 1000px;}</style>
<script src="http://knowledgevault.saecanet.com/components/JavascriptCode.js"></script>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 1000)
```

```{r,warning=F,error=F,message=F,echo=F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", functionList[iii,1]),
    ssl.verifypeer = F
  )
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,echo=F}
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/importJobOfferByTermsOfOccupationalCategories.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))
fun_jobOffer(dataID = '000031592164', sheetNo = 14)
colnames(jobOffer) <- gsub('\n','',colnames(jobOffer))
jobOffer[,-1] <- apply(jobOffer[,-1],2,as.numeric)
```

```{r warning=F,error=F,message=F,echo=F,results='hold'}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = jobOffer, dateFormat = dateFormat, objColumn = 2)
dataTitle <- 
  paste0('職業別労働市場関係指標(実数,平成23年改定):', 
         zen2han(sheetTitle2),':',latestResult)
dataSource <- '厚生労働省'
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.e-stat.go.jp/SG1/estat/NewList.do?tid=000001020327
- 職業の大分類と中分類が混在しています。

***

```{r warning=F,error=F,message=F,echo=F,results='hold'}
tailN <- 4
buf <-
  fun_transposeDataFrame(obj = jobOffer,dateFormat = dateFormat,colName = '職業',tailN = tailN)
under1 <-
  subset(buf, buf[,ncol(buf)] < 1)
over1 <-
  subset(buf, buf[,ncol(buf)] >= 1)
```

#### 直近の有効求人倍率が1.0以上の職業:直近`r tailN`ヶ月

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_dataTable(obj = over1,needID = 1,leftcolumns = 2,dateFormat = 0,orderDirection = 'asc')
```

***
#### 直近の有効求人倍率が1.0未満の職業:直近`r tailN`ヶ月

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_dataTable(obj = under1,needID = 1,leftcolumns = 2,dateFormat = 0,orderDirection = 'asc')
```

***
#### 直近の有効求人倍率上位10:直近`r tailN`ヶ月

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = head(buf[order(buf[,ncol(buf)],decreasing = T),],10))
```

***
#### 直近の有効求人倍率下位10:直近`r tailN`5ヶ月

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = head(buf[order(buf[,ncol(buf)],decreasing = F),],10))
```

***
#### 職業別有効求人倍率:最高倍率職業の推移("分類不能の職業"除く)

```{r warning=F,error=F,message=F,echo=F,results='asis'}
obj <-
  jobOffer[,-grep('分類不', colnames(jobOffer))]
topJob <-
  apply(obj,1,function(x)paste0(colnames(obj)[which(max(x)==x)],collapse = ' , '))
topJobRate <- 
  as.numeric(apply(obj,1,function(x)max(x)))
topJobDF <- 
  data.frame(obj[,1],topJob,topJobRate)
colnames(topJobDF) <- 
  c('Date','最高有効求人倍率の職業','有効求人倍率')
fun_dataTable(obj = topJobDF,
              title = paste0(format(range(topJobDF[,1]),dateFormat),collapse = '~'),
              leftcolumns = 2,
              dateFormat = dateFormat)
```

***
#### 職業別有効求人倍率:最低倍率職業の推移("分類不能の職業"除く)

```{r warning=F,error=F,message=F,echo=F,results='asis'}
obj <-
  jobOffer[,-grep('分類不', colnames(jobOffer))]
topJob <-
  apply(obj,1,function(x)paste0(colnames(obj)[which(min(x)==x)],collapse = ' , '))
topJobRate <- 
  as.numeric(apply(obj,1,function(x)min(x)))
topJobDF <- 
  data.frame(obj[,1],topJob,topJobRate)
colnames(topJobDF) <- 
  c('Date','最低有効求人倍率の職業','有効求人倍率')
fun_dataTable(obj = topJobDF,
              title = paste0(format(range(topJobDF[,1]),dateFormat),collapse = '~'),
              leftcolumns = 2,
              dateFormat = dateFormat)
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_preparePlot()
pngFile <-
  paste0(htmlName, "1.png")
png(file = pngFile, width = 1000, height = 600)
##################
objColumn <- 
  which(max(unlist(tail(jobOffer[,-1],1)))==unlist(tail(jobOffer[,-1],1))) + 1
obj <- jobOffer[,c(1,objColumn)]
fun_plotTimeSeries(obj = obj,
                   chartType = 0,
                   dataTitle = zen2han(sheetTitle2),
                   dateFormat = dateFormat,
                   dataSource = dataSource,
                   mar = c(4,4,5,2),
                   fitlwdL = 2,
                   fitLcolor = 2,
                   fitlwdR = 2,
                   fitRcolor = 2,
                   needLefLab = 0,
                   typeL = 'o',
                   typeR = 'o',
                   cexL = 1.0, 
                   cexR = 1.0,  
                   cex.legend = 2,
                   cex.main = 1.1)
abline(h=0,col='red',lwd=2)
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
objColumn <- 
  grep('管理的職業|技術的職業$|事務的職業$|^[^:]+の職業$|介護関係',colnames(jobOffer))
obj <- jobOffer[,c(1,objColumn)]
fun_transposeDataFrame(obj = obj,dateFormat = dateFormat,colName = '職種')
#########################
outputDF <- 
  fun_transposeDataFrame(obj = jobOffer,
                         tailN = 1,
                         dateFormat = dateFormat,
                         colName = sheetTitle2)
outputDF <- 
  outputDF[order(outputDF[,2],decreasing = T),]
fun_outputMD(tags = '厚生労働省,有効求人倍率',
             objDF = outputDF,
             htmlName = htmlName,
             image1 = 1,
             title = dataTitle,
             tableTitle = colnames(outputDF)[2],
             tableName = colnames(outputDF)[1])
fun_updateURLcsv()
```

***

<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
