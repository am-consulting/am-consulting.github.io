---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'SalesByIndustryOfBusinessActivity';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : 'Sales By Industry Of Business Activity'
author: 'アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp'
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 5
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers 
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<script src="http://knowledgevault.saecanet.com/components/JavascriptCode.js"></script>
<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 1000)
```

```{r warning = F, error = F, message = F, echo = F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
scriptFile <- 'R-downloadFromESTAT.r'
script <-
  getURL(
    paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/",
           scriptFile),
    ssl.verifypeer = FALSE)
eval(parse(text = script))
fun_downloadFromESTAT(tclassID = '000001047452',targetFile = 1)
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
scriptFile <- 'importSalesByIndustryOfBusinessActivity.r'
script <-
  getURL(
    paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/",
           scriptFile),
    ssl.verifypeer = FALSE)
eval(parse(text = script))
fun_SalesByIndustryOfBusinessActivity(fileName = fileNameFromESTAT)
```

```{r warning=F,error=F,message=F,results='hold',echo=F}
obj <- SalesByIndustryOfBusinessActivity
obj[,-1] <- 
  data.frame(apply(obj[,-1],2,function(x)x*10^-6),check.names = F,stringsAsFactors = F)
colnames(obj) <- 
  gsub('百万','兆',colnames(obj))
```

```{r warning=F,error=F,message=F,results='hold',echo=F}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = obj,dateFormat = dateFormat)
dataTitle <- 
  paste0("サービス産業動向調査:",sheetTitle,':',latestResult)
dataSource <- "総務省"
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
dataSet <- 
  obj[,which(regexpr(':',gsub(':実数','',colnames(obj))) == -1)]
for(ccc in 2:ncol(dataSet)){
  fun_plotTimeSeries(obj = dataSet[,c(1,ccc)],
                     typeL = 'o',dateFormat = dateFormat,
                     chartType = 0,dataTitle = '事業活動の産業(中分類)別売上高',
                     dataSource = dataSource,fitLcolor = 2,fitlwdL = 3,
                     mar = c(5,5,6,2),cexL = 1.5)
  cat('<hr>')
}
```

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = fun_transposeDataFrame(obj = dataSet,dateFormat = dateFormat,tailN = 4))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
username <-
  Sys.info()['user']
pathToFile <-
  paste0('C:/Users/', username,'/Desktop/pathToCSV/')
setwd(pathToFile)
fileName <-
  'defaultPath.csv'
buf <-
  read.csv(file = fileName,
           header = F,
           skip = 0,
           stringsAsFactor = F,
           check.names = F,
           fileEncoding = 'utf-8',quote = "\"")
pathOutputTOcsv <-
  paste0("C:/Users/", username, buf[2,1],'charts/chartImages/')
setwd(pathOutputTOcsv)
getwd()
par(family = 'Meiryo')
pngFile <-
  paste0(htmlName, ".png")
png(file = pngFile, width = 1000, height = 600)
##################
dataSet <- 
  obj[,c(1,which(regexpr(':',gsub(':実数','',colnames(obj))) != -1))]
buf <- 
  fun_transposeDataFrame(obj = dataSet,tailN = 1,dateFormat = dateFormat)
latestDF <-
  buf[order(buf[,2],decreasing = T),]
buf <-
  obj[,c(1,grep(paste0(gsub('\\(.+\\)','',head(latestDF[,1],2)),collapse = '|'),colnames(obj)))]
fun_plotTimeSeries(obj = buf,
                   typeL = 'o',typeR = 'o',
                   dateFormat = dateFormat,
                   chartType = 1,dataTitle = '事業活動の産業(中分類)別売上高',
                   dataSource = dataSource,
                   fitLcolor = 1,fitlwdL = 3,cexL = 1.5,
                   fitRcolor = 2,fitlwdR = 3,cexR = 1.5,
                   mar = c(5,4,6,2),needLefLab = 0)

abline(h = 0,col = 'red',lwd = 2)
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_outputMD(tags = '総務省,サービス産業動向調査,産業(中分類)別売上高',
             objDF = latestDF,
             htmlName = htmlName,
             image = 1,
             dataTitle = dataTitle,
             tableTitle = 'サービス産業動向調査:産業(中分類)別売上高',
             tableName = 'サービス産業動向調査:産業(中分類)別売上高')
```

***
<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
