---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'InternationalBalanceofPayments';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[4,1],'am-consulting.co.jp-',htmlName,'.html')) })

title : 'International Balance of Payments of Japan'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 4
    fig_width: 9
    keep_md: no
    md_extensions: -ascii_identifiers 
    includes:
       in_header: insertHeader.html
    self_contained: false
---

```{r setup, include=FALSE}
tag <-
  read.csv(file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/components/insertHeader.csv',header = F,fileEncoding = 'UTF8')
tagTxt <- ''
for(rrr in 1:nrow(tag)){
  tagTxt <- paste0(tagTxt,tag[rrr,1],'\n')
}
insertFile <- file("insertHeader.html")
writeLines(text = tagTxt,con = insertFile)
close(insertFile)
```

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
# ―,ソ,噂,欺,圭,構,蚕,十,申,貼,能,表,暴,予,禄,兔
options(width = 900)
```

```{r,warning=F,error=F,message=F,echo=F}
library(rAmCharts)
windowsFonts(Meiryo=windowsFont("Meiryo"))
options(scipen=100)
```

```{r,warning=F,error=F,message=F,echo=F}
script <-
  RCurl::getURL(
    "https://raw.githubusercontent.com/am-consulting/Rscript/master/importJapansBalanceofPayments.r",
    ssl.verifypeer = FALSE
  )
eval(parse(text = script))
```

```{r warning = F, error = F, message = F, echo = F,results='hide'}
# チェック用チャンク
for(iii in 1:4){
  print(tail(get(paste0('origData',iii))[,c(1,2,3)],2))
}
```

```{r warning = F, error = F, message = F, echo = F}
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    RCurl::getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = origData1,dateFormat = dateFormat,objColumn = c(2,4))
dataTitle <- 
  paste0('日本の国際収支:経常収支,資本移転等収支,金融収支,誤差脱漏,貿易収支,サービス収支,所得収支,輸出,輸入(億円):', latestResult) 
dataSource <- '財務省'
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.mof.go.jp/international_policy/reference/balance_of_payments/index.htm

***

```{r,warning=F,error=F,message=F,echo=F}
funamBarplot <- function(obj, mainTitle = '') {
  amBarplot(
    x = colnames(obj)[1] ,
    y = colnames(obj)[2:ncol(obj)],
    data = obj,
    horiz = T,
    stack_type = 'regular',
    legend = T,
    labelRotation = 0,
    main = mainTitle,
    mainColor = "#68838B",
    mainSize = 15,
    creditsPosition = "top-right",
    legendPosition = "bottom",
    show_values = T
  )
}  
```

####日本の国際収支(単位:億円)
- 注)経常収支＋資本移転等収支－金融収支＋誤差脱漏＝0
- 注)金融収支の額は棒グラフに表示されている数値の符号を逆にして読み取って下さい。
- Powered by https://www.amcharts.com/

```{r,warning=F,error=F,message=F,results='asis',echo=F}
bop0 <- origData1[, c(1, 2, 10, 11, 17), drop = F]
bop0[, 1] <- as.character(format(bop0[, 1], dateFormat))
bop <- tail(bop0, 12)
bop[, 4] <- bop[, 4] * -1
funamBarplot(obj = bop, mainTitle = '日本の国際収支(単位：億円)' )
```

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj <- origData1[, c(1, 2, 10, 11, 17)]
# fun_dataTable(obj = tail(obj,5*12),dateFormat =dateFormat)
```

***
##### 経常収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
fun_withList(obj = obj)
fun_withWiki(obj = dfWithList)
obj0 <- dfWithWiki[,-c(3:5,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
##### 金融収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj0 <- dfWithWiki[,-c(2,3,5,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
##### 資本移転収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj0 <- dfWithWiki[,-c(2,4,5,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
#### 日本の経常収支(単位:億円)
- Powered by https://www.amcharts.com/

```{r,warning=F,error=F,message=F,results='asis',echo=F}
currentAccount0 <- origData1[, c(1,4,7:9), drop = F]
currentAccount0[, 1] <- as.character(format(currentAccount0[, 1], dateFormat))
currentAccount <- tail(currentAccount0, 12)
funamBarplot(obj = currentAccount, mainTitle = '日本の経常収支(単位：億円)' )
```

```{r,warning=F,error=F,message=F,results='asis',echo=F}
# fun_dataTable(obj = tail(currentAccount0,5*12),dateFormat = 0)
```

***
##### 貿易収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj <- origData1[, c(1,4,7:9)]
fun_withList(obj = obj)
fun_withWiki(obj = dfWithList)
obj0 <- dfWithWiki[,-c(3:5,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
##### サービス収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj0 <- dfWithWiki[,-c(2,4,5,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
##### 第一次所得収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj0 <- dfWithWiki[,-c(2,3,5,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
##### 第二次所得収支の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj0 <- dfWithWiki[,-c(2,3,4,11:12)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
##### 経常収支の時系列推移(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
currentAccount0 <- origData1[, c(1,2), drop = F]
n <- 1
fun_generateKnit(objDF = currentAccount0,plotNum = n,group = 0,barPlot = 1)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', ConsumptionTax)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', event_dygraph_point,' %>%  ',event_dygraph_range)))
```

***

#### 日本の貿易収支(単位:億円)
- 注)貿易収支＝輸出 －輸入
- 注)輸入の額は棒グラフに表示されている数値の符号を逆にして読み取って下さい。
- Powered by https://www.amcharts.com/

```{r,warning=F,error=F,message=F,results='asis',echo=F}
goods0 <- origData1[, c(1,5:6), drop = F]
goods0[, 1] <- as.character(format(goods0[, 1], dateFormat))
goods <- tail(goods0, 12)
goods[, 3] <- goods[, 3] * -1
funamBarplot(obj = goods, mainTitle = '日本の貿易収支(単位：億円)' )
```

```{r,warning=F,error=F,message=F,results='asis',echo=F}
# fun_dataTable(obj = tail(goods0,5*12),dateFormat = 0)
```

***

##### 貿易収支の時系列推移(単位:億円) 

```{r,warning=F,error=F,message=F,results='asis',echo=F}
goods0 <- origData1[, c(1,4:6), drop = F]
n <- 2
fun_generateKnit(objDF = goods0,plotNum = n,group = 0)
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', ConsumptionTax)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', boj_dygraph)))
cat('<hr>')
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', event_dygraph_point,' %>%  ',event_dygraph_range)))
```

***
##### 貿易輸出の最高額と最低額(単位:億円)

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj <- origData1[, c(1,5:6)]
fun_withList(obj = obj)
fun_withWiki(obj = dfWithList)
cat('- 輸出\n')
obj0 <- dfWithWiki[,-c(3,9,10)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
cat('<hr>')
cat('- 輸入\n')
obj0 <- dfWithWiki[,-c(2,9,10)]
obj0 <- rbind(subset(obj0,max(obj0[,2])==obj0[,2]),subset(obj0,min(obj0[,2])==obj0[,2]))
obj0[,1] <- format(obj0[,1],dateFormat)
cat(paste0('- ',paste0(format(range(dfWithWiki[,1]),dateFormat),collapse = ' ~ '),'\n'))
fun_markdownTable(obj = obj0)
```

***
#### 国際収支個別系列:直近3ヶ月

```{r,warning=F,error=F,message=F,results='asis',echo=F}
obj3 <- merge(origData3,origData4,by='Date')
obj2 <- merge(origData2,obj3,by='Date')
obj1 <- origData1
fun_markdownTable(obj = fun_transposeDataFrame(obj = obj1,dateFormat = dateFormat,tailN = 3))
cat('<hr>')
fun_markdownTable(obj = fun_transposeDataFrame(obj = obj2,dateFormat = dateFormat,tailN = 3))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
username <- Sys.info()['user']
pathToFile <- paste0('C:/Users/', username,'/Desktop/pathToCSV/')
setwd(pathToFile)
fileName <- 'defaultPath.csv'
buf <-
  read.csv(file = fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,
           fileEncoding = 'utf-8',quote = "\"")
pathOutputTOchart <- paste0("C:/Users/", username, buf[4,1])
setwd(pathOutputTOchart)
pngFile <-
  paste0(htmlName, "1.png")
png(file = pngFile, width = 1000, height = 600)
#---------------------------------------------------
obj <- na.omit(origData1[,c(1,5)])
obj[,2] <- obj[,2]*10^-4
colnames(obj)[2] <- paste0(colnames(obj)[2],'(兆円)')
caption.subtitle.size <- 14
plot.text.size <- 13
base.size <- 11
col <- c('#4169e1','#00008b','#6495ed')
fun_plot <- function(obj.col = 2){
  date.range <- paste0(format(range(obj[,1]),dateFormat),collapse = '~')
  g <- ggplot(data = obj,aes(x = obj[,1],y = obj[,obj.col]))
  g <- g + theme_grey(base_size = base.size,base_family = 'Meiryo')
  g <- g + geom_line(col=col[obj.col-1]) + geom_point(col=col[obj.col-1]) + geom_smooth(col='red')
  g <- g + ggtitle(label = paste0(colnames(obj)[obj.col],
                                  '\n',date.range,'\nSource:',dataSource))
  g <- g + scale_y_continuous(name = '',breaks = scales::pretty_breaks(20),sec.axis = dup_axis())
  g <- g + xlab(label = '')
  g <- g + theme(plot.title = element_text(hjust = 0.5))
  g <- g + theme(axis.text.x = element_text(size = plot.text.size,angle = 0))
  g <- g + theme(axis.text.y = element_text(size = plot.text.size,angle = 0))
  g <- g + theme(plot.subtitle = element_text(size = caption.subtitle.size,angle = 0))
  g <- g + theme(plot.caption = element_text(size = caption.subtitle.size,angle = 0))
  print(g)
}
fun_plot()
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_outputMD(tags = '国際収支,経常収支,貿易収支',
             objDF = fun_summaryByName(obj = obj,omitFirst = 1,dateFormat = dateFormat),
             htmlName = htmlName,
             image1 = 1,image2 = 0,
             title = dataTitle,
             tableTitle = colnames(obj)[2],
             buf.row = 4)
fun_updateURLcsv()
fun_updateURLcsv.olive()
```

***
<script>MakeNegative()</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
