---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'MonthlyProductiveCapacityAndOperatingRatio';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : "Monthly Productive Capacity And Operating Ratio In Japan"
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 3
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers 
    includes:
       in_header: insertHeader.html
    self_contained: false
---

```{r setup, include=FALSE}
tag <-
  read.csv(file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/components/insertHeader.csv',header = F,fileEncoding = 'UTF8')
tagTxt <- ''
for(rrr in 1:nrow(tag)){
  tagTxt <- paste0(tagTxt,tag[rrr,1],'\n')
}
insertFile <- file("insertHeader.html")
writeLines(text = tagTxt,con = insertFile)
close(insertFile)
```

<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options,echo=F,cache=F}
options(width = 1000)
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
library(RCurl);library(knitr)
functionList <-
  read.csv(
    file = 'https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/functionList.csv',
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(
      paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", functionList[iii,1]),
      ssl.verifypeer = F
      )
  eval(parse(text = script))
}
```

```{r warning=F,error=F,message=F,echo=F}
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/importMonthlyProductiveCapacityAndOperatingRatio.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))
```

```{r warning=F,error=F,message=F,echo=F,results='hold'}
dateFormat <- '%Y年%m月'
diffRatioDF <- fun_makeDiffRatioTable(obj = MonthlyProductiveCapacityAndOperatingRatio)
fun_latestResult(obj = diffRatioDF,dateFormat = dateFormat)
dataTitle <-
  paste0('製造工業生産能力･稼働率指数 季節調整済指数:前月比(%):',latestResult)
dataSource <- '経済産業省'
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`

***

```{r warning=F,error=F,message=F,echo=F,results='asis',fig.height=4}
obj <- MonthlyProductiveCapacityAndOperatingRatio[,c(1,2,37,38)]
n1 <- 1
fun_generateKnit(objDF = obj,
                 colors = c('blue','red','green'),plotNum = n1,
                 dygraphTitle = paste0(colnames(obj)[-1],collapse = '×'))
eval(parse(text = paste0('dygraphPlot', n1, ' %>% ', primeMinisterOfJapan_dygraph)))
```

***

- Straw Broom Chart

```{r warning=F,error=F,message=F,echo=F,results='asis',fig.height=4}
n1 <- 2
fun_generateKnit(objDF = obj,
                 colors = c('blue','red','green'),plotNum = n1,strawBroom = 1,
                 dygraphTitle = paste0(colnames(obj)[-1],collapse = '×'))
eval(parse(text = paste0('dygraphPlot', n1, ' %>% ', primeMinisterOfJapan_dygraph)))
```

#### 季節調整値

```{r warning=F,error=F,message=F,echo=F,results='asis'}
tailN <- 5
fun_markdownTable(obj = fun_transposeDataFrame(obj = MonthlyProductiveCapacityAndOperatingRatio,
                                               dateFormat = dateFormat,
                                               colName = '産業',
                                               tailN = tailN),
                  rownames = F)
```

***
#### 前月比(%)
##### 直近前月比プラス

```{r warning=F,error=F,message=F,echo=F,results='asis'}
MoM <- 
  fun_transposeDataFrame(obj = diffRatioDF,
                         dateFormat = dateFormat,
                         colName = '産業',
                         tailN = tailN)
PositiveRow <-
  which(0 <= MoM[,ncol(MoM)])
fun_markdownTable(obj = MoM[PositiveRow,],
                  rownames = F)
```

***

##### 直近前月比マイナス

```{r warning=F,error=F,message=F,echo=F,results='asis'}
fun_markdownTable(obj = MoM[-PositiveRow,],
                  rownames = F)
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_preparePlot()
pngFile <-
  paste0(htmlName, "1.png")
png(file = pngFile, width = 1000, height = 600)
##################
obj <- MonthlyProductiveCapacityAndOperatingRatio[,c(1,2)]
fun_plot4charts(obj = obj,dateFormat = dateFormat,tailN = c(10^10,12*5,12*3,12*1))
dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
fun_outputMD(tags = '稼働率指数',
             objDF = fun_summaryByName(obj = obj,dateFormat = dateFormat),
             summaryDF = obj,
             dateFormat = '%Y-%m-%d',
             dateCol = 1,
             objCol = 2,
             htmlName = htmlName,
             image1 = 1,
             title = dataTitle,
             tableTitle = colnames(obj)[2])
fun_updateURLcsv()
```

***

<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
