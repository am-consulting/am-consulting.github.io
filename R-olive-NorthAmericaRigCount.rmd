---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-NorthAmericaRigCount.html')) })
title : "North America Rig Count"
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 7
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers 
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet"> 
<style>
.main-container {max-width : 1000px; margin-left : auto; margin-right : auto;}
table{white-space : nowrap;}
td.negative {color : red;}
</style>

<script>
<!-- # Reference http://www.hanselman.com/blog/MakingNegativeNumbersTurnRedUsingCSSAndJavascript.aspx -->
function MakeNegative() {
  TDs = document.getElementsByTagName('td');
  for (var i=0; i<TDs.length; i++) {
    var temp = TDs[i];
    if (temp.firstChild.nodeValue.indexOf('-') == 0) temp.className = "negative";
  }
}
</script>



- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan

本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>



```{r set-options, echo=F, cache=F}
options(width = 1000)
```

```{r,warning=F,error=F,message=F,echo=F}
library(dplyr)
library(lubridate)
library(psych)
library(Hmisc)
library(pastecs)
library(tseries)
library(RCurl)
```

```{r,warning=F,error=F,message=F,echo=F}
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/importNorthAmericaRotaryRigCount.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/R-dygraphsFunction.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))

script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/transposeDataFrame.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))

script <-
  getURL(
    "https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/timeSeriesDataFundamentalStatistics.r",
    ssl.verifypeer = F
  )
eval(parse(text = script))
```

```{r,warning=F,error=F,message=F,echo=F}
tmp <- northAmericaRotaryRigCount_BakerHughes[,c(1:3)]
dataSet <- na.omit(tmp)
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
dateFormat <- '%Y年%m月%d日'
dataTitle <-
  paste0('Baker Hughes:North America Rig Count Oil ans Gas / 北米リグカウント 石油とガス',' - ',
         format(last(dataSet[, 1]), dateFormat))
dataSource <- 'Baker Hughes'
notes <- '複数のワードで検索する際は半角スペースで区切ってください'
notes0 <- "表中、-9999はNAを意味します"
Author <- "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
# cat(paste("Title       : ", dataTitle, "\n"))
# cat(paste("Data Source : ", dataSource, "\n"))
# cat(paste("Author      : ", Author, "\n"))
# cat(paste("特  記      : ", notes, "\n"))
# cat(paste("特  記      : ", notes0, "\n"))
windowsFonts(Meiryo = windowsFont("Meiryo"))
options(scipen = 999)
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- `r paste0("特記 : ", notes)`
- `r paste0("特記 : ", notes0)`
- Source https://www.bakerhughes.com/

***

```{r warning=F, error=F, message=F, echo=F}
fun_plotTimeSeries <- 
  function(obj, objX = 1, objY01 = 2, objY02 = 3, type01 = 'l', type02 = 'l', dateFormat = '%Y-%m-%d', chartType = 2, period = '') {
if(chartType!=0){
mainTitle <- paste0(colnames(obj)[objY01],' × ',colnames(obj)[objY02])
maxValue <- max(obj[,objY01],obj[,objY02])
minValue <- min(obj[,objY01],obj[,objY02])
}else{ # 1系列のみの場合
mainTitle <- colnames(obj)[objY01]
}
par(mar=c(3,5,4,5),family = 'Meiryo')
plot(
  obj[, objX]  , obj[, objY01], type = type01, col = lineColor[1], xlab = '', ylab = '', xaxt = 'n', 
  ylim = if(chartType == 1){c(minValue, maxValue)},
  main = paste(mainTitle,'\n', period, '\nSource:',dataSource),
  cex.axis = 1 , cex.lab = 1,   cex.main = 1, lwd = 1
  )
lo <- loess(obj[, objY01] ~ as.numeric(obj[,objX]), degree = 2)
lines(obj[,objX] , predict(lo) , col = lineColor[1], lwd = 1, lty = 2)
panel.first = grid(nx = NULL, ny = NULL, lty = 2, equilogs = T)
if(chartType!=0){ # 2系列の場合
  if(chartType == 1){
  lines(obj[,objX], obj[,objY02], col = lineColor[2], lwd = 1, type = type02)
  }else{  
  par(new=T)
  plot(
    obj[, objX]  , obj[, objY02], type = type02, col = lineColor[2], xlab = '', ylab = '', xaxt = 'n', yaxt = 'n', 
    cex.axis = 1 , cex.lab = 1, cex.main = 1, lwd = 1
    )
  panel.first = grid(nx = NULL, ny = NULL, lty = 2, equilogs = T)
  axis(4, cex.axis = 1, cex.lab = 1)
  mtext(colnames(obj)[objY02], side=4, line=3.2, cex=1)
  }
  lo <- loess(obj[, objY02] ~ as.numeric(obj[,objX]), degree = 2)
  lines(obj[,objX] , predict(lo) , col = lineColor[2], lwd = 1, lty = 2)
  graphics::legend('topleft',col=lineColor,lty=1,legend=colnames(obj)[c(objY01,objY02)],cex=1,bty='n',lwd = 2)
}
axis.Date(side=1, at=obj[,objX],format=dateFormat,padj=1,cex.axis=1.0) 
mtext(colnames(obj)[objY01], side=2, line=3.2, cex=1)
  }
```

```{r,warning=F,error=F,message=F,echo=F}
funDataTable <- function(obj, orderColumn = 0, orderDirection = 'desc', width01 = '10%', caption = '') {
  DT::datatable(
    obj,
    extensions = c("Buttons", "ColReorder" , "FixedColumns", "FixedHeader"),
    options = list(paging = T, autoWidth = F, info = T, lengthChange = T, ordering = T,
      searching = T, scrollX = T, lengthMenu = list(c(10, 50, -1, 1), c(10, 50, 'All', 1)),
      orderClasses = T, order = list(list(orderColumn, orderDirection)), 
      search = list(regex = T, caseInsensitive = T), searchHighlight = T,
      columnDefs = list(list(width = width01, targets = 0)),
      dom = 'BRlfrtip', buttons = I('colvis'), colReorder = T, fixedColumns = list(leftColumns = 1), fixedHeader = T),
    rownames = F, caption = caption, class = 'display compact', escape = F, filter = 'top'
  )  
}
```

```{r,warning=F,error=F,message=F,results='asis',echo=F,fig.height=5,fig.width=10}
lineColor <- c('blue', 'red', 'black')
dataTail <- 12 * 20000
dataSet <- tail(dataSet, dataTail)
objX <- 1
objY01 <- 2
objY02 <- 3
datePeriod <- paste0(format(range(dataSet[,objX]), dateFormat),collapse = '~')
fun_plotTimeSeries(obj = dataSet, objX = objX, objY01 = objY01, objY02 = objY02, period = datePeriod)
```
<hr>
```{r,warning=F,error=F,message=F,results='asis',echo=F,fig.height=5,fig.width=10}
par(mfrow = c(1, 2), mar = c(3, 3, 3, 3), family = 'Meiryo', oma = c(0, 0, 3, 0))
for (ddd in 1:2) {
  bufN <- get(paste0('objY0', ddd))
  hist(
    dataSet[, bufN],
    col = '#ADD8E6',
    main = colnames(dataSet)[bufN],
    cex.main = 1.0
  )
}
mtext(datePeriod, side = 3,outer = T,cex = 1.2)
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
cat(datePeriod)
cat('\n\n')
summary(dataSet[,-1])
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
psych::describe(dataSet[,-1])
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
pastecs::stat.desc(dataSet[,-1]) 
```

```{r,warning=F,error=F,message=F,results='hold',echo=F}
apply(dataSet[,-1],2,adf.test)
```



```{r,warning=F,error=F,message=F,results='asis',echo=F}
dataSet0 <- dataSet
dataSet0 <- data.frame(dataSet0[-1,1,drop=F],round(diff(as.matrix(dataSet0[,-1,drop=F])),10),check.names = F)
colnames(dataSet0)[-1] <- paste0(colnames(dataSet0)[-1],'-Change From Last Data')
dataSet1 <- dataSet
dataSet1[,-1] <- apply(dataSet1[,-1,drop=F],2,function(x){round(x/max(x)*100,0)})
colnames(dataSet1)[-1] <- paste0(colnames(dataSet1)[-1],'-Ratio to Peak(%)')
dataSet0 <- merge(dataSet, dataSet0, by = 'Date',all = T)
dataSet0 <- merge(dataSet0, dataSet1, by = 'Date',all = T)
#dataSet0[is.na(dataSet0)] <- ''
#dataframeのNAを''に置換すると、そのNAを含む列全てがcharacterに変換されてしまうので注意
# dataSet0[, 1] <- format(dataSet0[, 1], dateFormat)
# funDataTable(obj = dataSet0, width01 = '15%')
```

```{r,warning=F,error=F,message=F,results='hide',echo=F}
knitr::kable(tail(dataSet,12),row.names = F)
```

```{r warning=F,error=F,message=F,echo=F,results='asis'}
knitr::kable(fun_transposeDataFrame(obj = dataSet0,tailN = 5),row.names = F)
```

***
#### Date when the positive and negative of week over week difference changed

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 4
dataSet2 <- dataSet0[-1,c(1,ccc)]
dataSet3 <- dataSet2[which(head(dataSet2[,2],-1) * tail(dataSet2[,2],-1) < 0) + 1,]
knitr::kable(tail(dataSet3,4),row.names = F)
```

***
```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 5
dataSet2 <- dataSet0[-1,c(1,ccc)]
dataSet3 <- dataSet2[which(head(dataSet2[,2],-1) * tail(dataSet2[,2],-1) < 0) + 1,]
knitr::kable(tail(dataSet3,4),row.names = F)
```

***
#### Each Max

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 2
knitr::kable(
  subset(dataSet0[,c(1,ccc)], max(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 3
knitr::kable(
  subset(dataSet0[,c(1,ccc)], max(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 4
knitr::kable(
  subset(dataSet0[,c(1,ccc)], max(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 5
knitr::kable(
  subset(dataSet0[,c(1,ccc)], max(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***
#### Each Min

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 2
knitr::kable(
  subset(dataSet0[,c(1,ccc)], min(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 3
knitr::kable(
  subset(dataSet0[,c(1,ccc)], min(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 4
knitr::kable(
  subset(dataSet0[,c(1,ccc)], min(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

```{r warning=F,error=F,message=F,echo=F,results='asis'}
ccc <- 5
knitr::kable(
  subset(dataSet0[,c(1,ccc)], min(dataSet0[,ccc],na.rm = T)==dataSet0[,ccc]),row.names = F)
```

***

<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
