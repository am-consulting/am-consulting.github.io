---
knit: (function(inputFile, encoding) {username<-Sys.info()['user'];fileName<-'defaultPath.csv';pathToFile <-paste0('C:/Users/', username,'/Desktop/pathToCSV/');setwd(pathToFile);buf0<-read.csv(fileName,header = F,skip = 0,stringsAsFactor = F,check.names = F,fileEncoding = 'utf-8');timeStamp<-format(Sys.time(),'%Y%m%d%H%M%S');htmlName<-'CurrentSurveyOfCommerce';rmarkdown::render(inputFile, encoding = encoding, output_file = paste0('C:/Users/',username,buf0[2,1],'charts/','am-consulting.co.jp-',htmlName,'.html')) })

title : 'Import Current Survey Of Commerce'
author: "アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp"
date: "`r format(Sys.time(), '%Y-%m-%d %H:%M:%S')`"
output: 
  html_document:
    fig_height: 4
    fig_width: 10
    keep_md: no
    md_extensions: -ascii_identifiers
---
<link href="http://knowledgevault.saecanet.com/components/rmarkdown-html.css" rel="stylesheet">
<script src="http://knowledgevault.saecanet.com/components/JavascriptCode.js"></script>
<style>.main-container {max-width : 1000px;}</style>

- アセット･マネジメント･コンサルティング株式会社 http://am-consulting.co.jp
- SaECaNet http://saecanet.com/
- Olive http://olive.saecanet.com/
- twitter https://twitter.com/AMC2_Japan
- 本ページへのリンクを共有:<a href="https://twitter.com/share" class="twitter-share-button" data-via="AMC2_Japan" data-size="large">Tweet</a> <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+'://platform.twitter.com/widgets.js';fjs.parentNode.insertBefore(js,fjs);}}(document, 'script', 'twitter-wjs');</script>

```{r set-options, echo=F, cache=F}
options(width = 1000)
```

```{r warning = F, error = F, message = F, echo = F}
library(RCurl)
fileName <- 'functionList.csv'
functionList <-
  read.csv(
    file = paste0('https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/csv/',
                  fileName),
    header = F,
    as.is = T)
for(iii in 1:nrow(functionList)){
  script <- 
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/", 
                  functionList[iii,1]),
           ssl.verifypeer = F)
  eval(parse(text = script))
}
```

```{r,warning=F,error=F,message=F,echo=F}
importAMCC <-c('importCurrentSurveyOfCommerce.r')
for(iii in 1:length(importAMCC)){
  script <-
    getURL(paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/",
                  importAMCC[iii]),
           ssl.verifypeer = F
  )
  eval(parse(text = script))
}
# 百貨店
returnList <- fun_CurrentSurveyOfCommerce(sheet = '百貨店　販売額　月次')
tmp <- returnList$CurrentSurveyOfCommerce
tmp[,-1] <- apply(tmp[,-1],2,function(x)x*10^-2)
departmentStore <- tmp
# スーパー
returnList <- fun_CurrentSurveyOfCommerce(sheet = 'スーパー　販売額　月次')
tmp <- returnList$CurrentSurveyOfCommerce
tmp[,-1] <- apply(tmp[,-1],2,function(x)x*10^-2)
superMarket <- tmp
# 合計
returnList <- fun_CurrentSurveyOfCommerce(sheet = '合計　販売額　月次')
tmp <- returnList$CurrentSurveyOfCommerce
tmp[,-1] <- apply(tmp[,-1],2,function(x)x*10^-2)
totalSales <- tmp
# "合計　販売額　月次"      
# "百貨店　販売額　月次"    
# "スーパー　販売額　月次"  
# "合計　伸び率　月次"      
# "百貨店　伸び率　月次"    
# "スーパー　伸び率　月次"  
# "合計　既存店　月次"      
# "百貨店　既存店　月次"    
# "スーパー　既存店　月次"
```

```{r warning=F,error=F,message=F,echo=F,results='hold'}
# csv出力パート
scriptFile <- 'R-writeCSVtoFolder.r'
script <-
  RCurl::getURL(
    paste0("https://raw.githubusercontent.com/am-consulting/am-consulting.github.io/master/",
           scriptFile),
    ssl.verifypeer = F)
eval(parse(text = script))
# csv出力パート
fun_writeCSVtoFolder(objData = departmentStore,dataType = 1,csvFileName = '商業動態統計調査_百貨店')
fun_writeCSVtoFolder(objData = superMarket,dataType = 1,csvFileName = '商業動態統計調査_スーパー')
fun_writeCSVtoFolder(objData = totalSales,dataType = 1,csvFileName = '商業動態統計調査_百貨店スーパー合計')
```

```{r warning=F,error=F,message=F,echo=F,results='hold'}
dateFormat <- '%Y年%m月'
fun_latestResult(obj = totalSales,objColumn = c(2), dateFormat = dateFormat)
dataTitle <- 
  paste0('商業動態統計調査:百貨店・スーパー商品別販売額(単位:億円):百貨店+スーパー:',latestResult)
dataSource <- '経済産業省'
windowsFonts(Meiryo = windowsFont("Meiryo"))
```

***

- `r paste0("タイトル : ", dataTitle)`
- `r paste0("データ出所 : ", dataSource)`
- Source http://www.e-stat.go.jp/SG1/estat/GL08020101.do?_toGL08020101_&tstatCode=000001081875

***

#### 商品別販売額(単位:億円):百貨店+スーパー

```{r warning=F, error=F, message=F, echo=F,results='asis',fig.height=12}
par(mfrow=c(5,3),oma=c(0,0,0,0))
obj <- totalSales
for(ccc in 2:ncol(obj)){
  fun_plotTimeSeries(obj = obj[,c(1,ccc)],typeL = 'o',dateFormat = dateFormat,
                     chartType = 0,dataSource = dataSource,fitLcolor = 2,pchL = 20,
                     fitlwdL = 3,
                     cexL = 1,lwdL = 1,mar = c(3,3,4,1),needLefLab = 0,dataTitle = '百貨店+スーパー')
}
```

***
#### 商品別販売額(単位:億円):百貨店

```{r warning=F, error=F, message=F, echo=F,results='asis',fig.height=12}
par(mfrow=c(5,3),oma=c(0,0,0,0))
obj <- departmentStore
for(ccc in 2:ncol(obj)){
  fun_plotTimeSeries(obj = obj[,c(1,ccc)],typeL = 'o',dateFormat = dateFormat,
                     chartType = 0,dataSource = dataSource,fitLcolor = 2,pchL = 20,
                     fitlwdL = 3,
                     cexL = 1,lwdL = 1,mar = c(3,3,4,1),needLefLab = 0,dataTitle = '百貨店')
}
```

***
#### 商品別販売額(単位:億円):スーパー

```{r warning=F, error=F, message=F, echo=F,results='asis',fig.height=12}
par(mfrow=c(5,3),oma=c(0,0,0,0))
obj <- superMarket
for(ccc in 2:ncol(obj)){
  fun_plotTimeSeries(obj = obj[,c(1,ccc)],typeL = 'o',dateFormat = dateFormat,
                     chartType = 0,dataSource = dataSource,fitLcolor = 2,pchL = 20,
                     fitlwdL = 3,
                     cexL = 1,lwdL = 1,mar = c(3,3,4,1),needLefLab = 0,dataTitle = 'スーパー')
}
```

***
#### 前年同月比(%):百貨店+スーパー

```{r warning=F, error=F, message=F, echo=F,results='asis'}
obj <- totalSales[,c(1,2)]
n <- 1
fun_generateKnit(objDF = fun_makeDiffRatioTable(obj = obj,lag = 12),plotNum = n,
                 barPlot = 1,group = 0,quantiles = 2,
                 dygraphTitle = paste0('前年同月比(%):百貨店+スーパー:',colnames(obj)[2]))
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
```

***
#### 前年同月比(%):百貨店

```{r warning=F, error=F, message=F, echo=F,results='asis'}
obj <- departmentStore[,c(1,2)]
n <- 1
fun_generateKnit(objDF = fun_makeDiffRatioTable(obj = obj,lag = 12),plotNum = n,
                 barPlot = 1,group = 0,quantiles = 2,
                 dygraphTitle = paste0('前年同月比(%):百貨店:',colnames(obj)[2]))
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
```

***
#### 前年同月比(%):スーパー

```{r warning=F, error=F, message=F, echo=F,results='asis'}
obj <- superMarket[,c(1,2)]
n <- 1
fun_generateKnit(objDF = fun_makeDiffRatioTable(obj = obj,lag = 12),plotNum = n,
                 barPlot = 1,group = 0,quantiles = 2,
                 dygraphTitle = paste0('前年同月比(%):スーパー:',colnames(obj)[2]))
eval(parse(text = paste0('dygraphPlot', n, ' %>% ', primeMinisterOfJapan_dygraph)))
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
obj <- totalSales[,c(1,2)]
colnames(obj)[2] <- paste0('百貨店･スーパー:',colnames(obj)[2],'(億円)')
fun_PlotStrawAnd4charts(obj = obj,dateFormat = dateFormat)
dev.off();dev.off()
```

```{r warning=F,error=F,message=F,echo=F,results='hide'}
tDF <- fun_transposeDataFrame(obj = totalSales,dateFormat = dateFormat,colName = '百貨店+スーパー(億円)')
obj <-
  tDF[order(tDF[,ncol(tDF)],decreasing = T),]
fun_outputMD(tags = '商業動態統計調査,:百貨店,スーパー,商品別販売額,経済産業省',
             objDF = obj,
             htmlName = htmlName,
             image1 = 1,image2 = 1,
             title = dataTitle,
             tableTitle = '直近5ヶ月',
             tableName = '百貨店+スーパー(億円)')
fun_updateURLcsv()
```

***
<script>MakeNegative();</script>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-3077339-5', 'auto');
  ga('send', 'pageview');

</script>
